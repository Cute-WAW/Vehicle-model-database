# LightGBM 模型改造实施清单

## 文档说明

本文档用于跟踪二手车残值评估主模型从 `curve` 迁移到 `LightGBM` 的实施进度。

- 当前状态以复选框维护
- 每个阶段包含目标、任务、交付物、验收点
- 文档默认以“保留旧模型并存、逐步切换”为实施策略

## 项目目标

- 用 `LightGBM` 替换当前主残值模型预测能力
- 保持 `/predict` 请求和响应结构兼容
- 保留相似车修正逻辑
- 保留 `C2B2C` 价格矩阵生成逻辑
- 保留旧 `curve` 模型回退能力

## 成功标准

- 离线评估结果优于或不劣于当前 `curve` 模型
- 主接口字段保持兼容：`predicted_price`、`residual_rate`、`price_matrix`、`debug`
- 支持通过配置或代码开关切换 `curve` / `lightgbm`
- 关键测试、部署验证和回滚方案可用

## 总体进度

- [x] 现状调研完成
- [ ] 改造方案冻结
- [ ] 离线 POC 完成
- [ ] 代码接入完成
- [ ] 回归验证完成
- [ ] 默认模型切换完成

---

## 阶段 1：冻结当前基线

### 目标

建立当前 `curve` 模型的结构认知、运行路径和性能基线，避免后续迁移时失去参照。

### 任务

- [x] 梳理当前主模型链路、模型目录、训练入口、预测入口
- [ ] 记录当前 `curve` 模型基线指标
- [ ] 统一确认训练输出目录与运行时读取目录
- [ ] 补一份当前模型行为说明，明确优先级与回退逻辑

### 当前已确认事实

- 当前主模型为曲线拟合模型，候选函数包括 `Exponential`、`Polynomial`、`Power`
- 当前预测流程优先走品牌车系模型，失败后回退车型类别模型
- 当前运行时默认读取 `output/brand_series_models` 和 `output/car_types_models`
- 当前训练脚本默认输出目录与运行目录存在历史分叉，需要统一
- `C2B2C` 价格矩阵为独立辅助模型，不在本阶段替换范围内

### 交付物

- [ ] 当前模型基线报告
- [ ] 当前模型训练/加载目录约定说明

### 验收点

- [ ] 能明确回答“当前线上到底加载哪类模型、从哪里加载”
- [ ] 能给出后续对照实验所需的旧模型指标

---

## 阶段 2：冻结 LightGBM 建模方案

### 目标

明确首版 LightGBM 的目标变量、特征集、训练策略和业务约束，避免实现时临时拍板。

### 任务

- [x] 冻结首版目标变量为“残值率”
- [x] 冻结首版采用“预测残值率，再换算价格”
- [x] 冻结首版特征集
- [x] 冻结首版保留相似车修正
- [x] 冻结首版保留 `C2B2C` 价格矩阵
- [x] 冻结首版保留旧模型并存与回退能力
- [ ] 确认最终模型开关策略
- [ ] 确认目录与命名规范

### 首版特征集

数值特征：

- `使用年限`
- `新车的价格`
- `行驶里程`

类别特征：

- `品牌车系`
- `车辆评级`
- `车辆大类`
- `车辆小类`
- `车辆属性`
- `城市`

### 首版约束

- [x] 使用 `LightGBMRegressor`
- [x] 对 `使用年限` 设置单调递减约束
- [x] 输出残值率裁剪到 `[0, 1]`
- [ ] 记录异常预测样本并评估比例

### 交付物

- [ ] LightGBM 特征与目标定义说明
- [ ] 训练策略说明

### 验收点

- [ ] 实现前无需再对“预测残值率还是价格”做决策
- [ ] 实现前无需再对“首版是否保留旧模型并存”做决策

---

## 阶段 3：新增模型封装与训练流程

### 目标

新增一套与旧模型兼容的 LightGBM 模型封装和训练脚本，减少对线上主逻辑的侵入。

### 任务

- [ ] 新增 `LightGBMResidualModel` 封装
- [ ] 支持 `save()` / `load()`，并保存特征元数据
- [ ] 支持 `predict(...)`
- [ ] 支持 `predict_price(...)`
- [ ] 保留 `model_type`、`r2`、`rmse`、`sample_count` 等调试字段
- [ ] 保留 `get_formula()` 风格接口，但对 LightGBM 返回固定说明文本
- [ ] 新增 LightGBM 训练脚本
- [ ] 完成数据清洗、特征构建、训练/验证集划分、训练、评估、序列化
- [ ] 明确新模型输出目录

### 交付物

- [ ] LightGBM 模型封装
- [ ] LightGBM 训练脚本
- [ ] 新模型文件格式说明

### 验收点

- [ ] 单独加载 LightGBM 模型文件即可完成预测
- [ ] 模型文件包含运行时所需的全部元数据

---

## 阶段 4：接入预测主链路

### 目标

让现有预测链路同时支持 `curve` 与 `lightgbm`，并保持 API 兼容。

### 任务

- [ ] 修改预测加载层，支持双模型
- [ ] 增加模型类型开关
- [ ] 默认先不切换主模型
- [ ] 保持品牌车系优先、车型类别兜底逻辑
- [ ] 保持新车价推断逻辑
- [ ] 保持相似车检索和调整策略
- [ ] 保持 `C2B2C` 价格矩阵逻辑
- [ ] 更新 `debug` 字段，区分 `curve` / `lightgbm`

### 交付物

- [ ] 双模型兼容版预测器
- [ ] 模型切换配置说明

### 验收点

- [ ] `/predict` 请求结构不变
- [ ] `/predict` 响应结构不变
- [ ] 使用开关即可切换不同模型

---

## 阶段 5：评估、测试与切换准备

### 目标

完成新旧模型对比、测试补齐和部署验证，达到可切换和可回滚状态。

### 任务

- [ ] 补充离线对比评估脚本
- [ ] 输出新旧模型对照报告
- [ ] 增加分桶分析
- [ ] 更新模型单测
- [ ] 更新序列化测试
- [ ] 更新端到端预测测试
- [ ] 校验 Docker 环境安装 `lightgbm`
- [ ] 校验本地环境安装与加载
- [ ] 更新训练、部署、切换、回滚文档

### 推荐分桶

- [ ] 车龄段
- [ ] 品牌车系
- [ ] 新能源 / 燃油
- [ ] 高里程 / 低里程
- [ ] 极端低价 / 高价

### 交付物

- [ ] 新旧模型对比报告
- [ ] 更新后的测试说明
- [ ] 部署与回滚文档

### 验收点

- [ ] LightGBM 离线表现达到替换条件
- [ ] 本地和 Docker 可运行
- [ ] 发现问题时可快速回退到旧模型

---

## 关键接口调整清单

### 保持兼容

- [ ] `/predict` 请求结构不变
- [ ] `/predict` 响应结构不变
- [ ] `predicted_price` 语义不变
- [ ] `residual_rate` 语义不变
- [ ] `price_matrix` 语义不变
- [ ] 相似车修正逻辑保留

### 需要调整

- [ ] 当前模型体系扩展为可表示 `curve` / `lightgbm`
- [ ] `PricePredictor` 支持双模型识别与加载
- [ ] `debug.model_type` 可显示 `LightGBM`
- [ ] LightGBM 场景下 `get_formula()` 返回说明文本而不是伪公式

---

## 功能测试清单

- [ ] 已有品牌车系模型命中时，LightGBM 能正常预测
- [ ] 品牌车系模型缺失时，兜底逻辑正常
- [ ] 相似车修正仍然有效
- [ ] `C2B2C` 价格矩阵仍然可生成
- [ ] `debug` 字段能区分 `curve` / `lightgbm`

## 质量测试清单

- [ ] 残值率输出始终在合法范围内
- [ ] 车龄增大时异常上涨样本比例可接受
- [ ] 老车、新能源车、典型品牌样本表现稳定
- [ ] 新模型不劣于旧模型基线

## 部署测试清单

- [ ] 本地环境安装通过
- [ ] Docker 镜像构建通过
- [ ] 容器启动后接口可正常预测
- [ ] 模型开关切换不影响接口稳定性

---

## 默认假设

- 首版目标变量为“残值率”
- 首版引入多特征 LightGBM，而不是仅用车龄
- 首版保留旧 `curve` 模型并存
- 首版保留相似车修正与 `C2B2C` 价格矩阵
- 模型切换能力为上线前必要能力
- 默认先完成离线 POC 和双模型兼容，再决定是否切主

---

## 进度记录

> 用于每次实现后补充实际进展。

### 2026-04-07

- 已完成现状调研
- 已整理 LightGBM 改造实施清单文档
- 待开始代码接入

